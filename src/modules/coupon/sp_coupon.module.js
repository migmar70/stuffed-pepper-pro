jQuery(document).ready( function($) {
  /*

	$('#coupon_add').button().click(function( e ){
		e.preventDefault();
		alert('got it');
	});
    $.post( ajaxurl, {
        action: 'sp_coupons_get', 
        user_id: 'user_id'
      }, 
      function( response ) {
      },
      'json'
    );
	*/
});
